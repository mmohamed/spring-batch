spring:
    jpa:
        hibernate.ddl-auto: none
    datasource:
        url: 'jdbc:mysql://localhost:3306/batch_test'
        username: 'batch'  
        password: 'batch'
    thymeleaf:
        mode: LEGACYHTML5  
        cache: false  
    batch:
        job.enabled: false
        initializer.enabled: true